from map import Map

if __name__ == '__main__':
    agent_count = 1
    # Iterate over games:
    while True:
        # Define (random) map sizes
        size_x = 6
        size_y = 6
        # obstacle_map = ...
        arena = Map(size_x=size_x, size_y=size_y, agent_count=agent_count, obstacle_map=None, next_step=False)

        # Set start and end positions
        # arena.set_aim_positions(...)
        # arena.set_current_positions(...)

        # TODO: To be continued
